paths:
    log: "runs"
    demo_sequence_data: "./data/test_dataset"
params:
        model_name: "HardNet128"
        weights_path: "./models/HardNet128.pth"
        image_size: 128
        patch_size: 64
        matching_threshold: 0.5
        distance_matching_threshold: 5

hydra:
    job:
        chdir: true